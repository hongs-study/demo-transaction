spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/Developer/datasource/h2/demo-transaction  # H2 DB ?? ?? (Embedded Mode)
    username: sa        # H2 DB ?? ?? (Embedded Mode)
    password:           # H2 DB ?? ID (??? ??)

  # H2 Console ??
  h2:
    console: # H2 DB? ??? ??? ? ?? ??
      enabled: true           # H2 Console ?? ??
      path: /h2-console       # H2 Console ?? ??

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create        # DB ??? ?? (none, create, create-drop, update, validate)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true      # ?? ?? ?? (??)
        show_sql: true        # ?? ?? ??

logging:
  level:
    root: info
    sql: debug
    org.springframework.transaction.interceptor: trace
